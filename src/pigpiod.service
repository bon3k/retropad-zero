[Unit]
Description=Pigpio Daemon
After=network.target

[Service]
Type=forking
PIDFile=/run/pigpio.pid

ExecStart=/usr/local/bin/pigpiod -f
ExecStop=/bin/kill -TERM $MAINPID
Restart=on-failure
User=root

[Install]
WantedBy=multi-user.target
